# Any category names work; use --config examples/batch.yaml --settings this file.
scenes:
  bioluminescent alien wetland: 1

generation:
  workflow: creative
  seed: null  # A new saved seed per run; set an integer for reproducible variations.
  quality: high
  frameworks: [blender, trimesh]
  blender_timeout: 1800
  llm:
    mode: live
    response_format: text
    stream: true
    timeout: 180
    max_tokens: 6000
    concurrency: 2
    requests_per_minute: 20
  assets:
    mode: polyhaven
    max_assets: 20
    texture_resolution: 2k
    max_download_bytes: 50000000
    max_total_bytes: 350000000
  output:
    preview: true
    require_blender: true
    preview_samples: 32
    preview_width: 1280
